name: Linux x86 Integration Test

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  call:
    uses: ./.github/workflows/_integration-test.yml
    with:
      runs-on: ubuntu-24.04
