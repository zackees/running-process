# Async API parity

This is the checked-in parity inventory for #850 and its completion follow-up
#875. It records, for every public operation of every process surface this
library ships, whether an async equivalent exists and which tests prove it.

The source of truth is [`docs/async_api_parity.toml`](async_api_parity.toml).
The table below is generated from it. Everything else on this page is prose that
explains how to read and change that manifest.

## How the manifest is enforced

`ci.parity_manifest` runs as part of `./lint` and the preflight lint job. It:

1. **Discovers** the public members of each tracked surface directly from the
   source — `ast` for the Python classes, an indentation scanner for the Rust
   `impl` blocks. A new public sync method with no manifest row fails the gate,
   and a row naming a member that no longer exists fails it too. That is the
   drift guard a hand-maintained markdown table could never be.
2. **Verifies the evidence** on every row marked `implemented`: each of the four
   columns must either name a test function that exists in this tree, or carry
   an `n/a: <rationale>` marker.
3. **Reports the RED set** — every row still marked `planned`, with the issue
   that owns it.

A slice shows its RED state before the change and its GREEN state after with:

```bash
uv run --no-sync python -m ci.parity_manifest --strict   # RED: every planned row fails
uv run --no-sync python -m ci.parity_manifest            # GREEN: gate passes
```

`--strict` is what makes the manifest a failing test for missing async
operations without painting CI red for work that is honestly still queued. When
the last row flips, `require_no_planned` in the manifest is set to `true` and
`--strict` becomes the permanent default.

After editing the manifest, regenerate this page:

```bash
uv run --no-sync python -m ci.parity_manifest --write
```

## The compatibility gates

The parity manifest answers "does async have this yet?". Three sibling gates
answer the question #875 cares about more — "did adding async break the sync
API?" — and all four run in `./lint` and the preflight lint job.

| Gate | Artefact | Fails when |
| --- | --- | --- |
| `ci.parity_manifest` | `docs/async_api_parity.toml` | a public member has no row, a row names a member that no longer exists, or a cited test does not exist |
| `ci.api_snapshot` | `docs/api_snapshot_python.txt`, `docs/api_snapshot_rust.txt` | the public sync surface changes — a renamed method, a reordered or renamed parameter, a changed default, a dropped export |
| `ci.sync_test_audit` | `docs/sync_test_baseline.txt` | a baselined synchronous test disappears |
| `ci.async_compliance_guard` | `platform_compliance_baseline.toml` | a raw platform API is used outside the blessed capability layer |

Regenerating any of them is one command, so an intentional change costs one
reviewable diff:

```bash
uv run --no-sync python -m ci.api_snapshot --write
uv run --no-sync python -m ci.sync_test_audit --write
uv run --no-sync python -m ci.parity_manifest --write
```

### Why a test baseline as well as an API snapshot

The API snapshot catches a changed signature. It cannot catch the quieter
failure: the signature stays, the behaviour regresses, and the test that would
have noticed was deleted along the way. `ci.sync_test_audit` is a one-way
ratchet on removal — adding tests never requires an update, so the baseline
stays a record of coverage rather than churn nobody reads.

The audit counts `#[test]` and `def test_*` only. `#[tokio::test]` and
`async def` are excluded on purpose: an audit that counted async tests as sync
coverage would report health while sync tests were being replaced by them,
which is precisely the substitution #875 asks us to rule out.

### Downstream fixtures

`tests/test_downstream_fixtures.py` runs FastLED-shaped consumer code in a
*fresh* interpreter: legacy imports, synchronous calls, and an event-loop policy
that raises if anything constructs a loop. A second fixture exercises sync and
async in one process in both orders, plus sync work from inside a running loop.

That file also carries a control test that splices a deliberate loop creation
into the fixture and requires it to fail. The first version of the loop
assertion inspected the policy's stored loop after the fact and never fired at
all — `new_event_loop()` builds a loop without installing it. Without the
control, "no loop was created" and "the check is broken" look identical.

## Compatibility rules

- Existing sync names, signatures, defaults, and import paths remain supported.
  A change to any of them needs a recorded justification under #849 and
  downstream review; the parity work is not a licence to rename.
- Async rows must resolve to native Rust futures or actor commands. Python
  executors, `asyncio.to_thread`, polling loops, and reader threads are
  forbidden, and `ci.async_compliance_guard` enforces that separately.
- An `n/a` rationale is a claim that the operation cannot meaningfully exist on
  that surface — not that nobody got to it. "Not implemented yet" is `planned`.

## Reading the table

`Status` is `implemented` when every applicable column below it names a real
test, and `planned` when async parity for that member is still outstanding. A
cell showing `-` on a planned row simply means the contract test has not been
written yet; on an implemented row every cell is filled.

<!-- BEGIN GENERATED PARITY TABLE -->

This table is generated from `docs/async_api_parity.toml` by `ci.parity_manifest`. Edit the manifest, then run `uv run --no-sync python -m ci.parity_manifest --write`.

### Rust `NativeProcess`

| Member | Status | Rust sync | Rust async | Python sync | Python async |
| --- | --- | --- | --- | --- | --- |
| `captured_combined` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | `test_streaming_reads_and_availability_agree` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently |
| `captured_combined_bytes` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | `test_streaming_reads_and_availability_agree` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently |
| `captured_stderr` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | `test_streaming_reads_and_availability_agree` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently |
| `captured_stdout` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | `test_streaming_reads_and_availability_agree` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently |
| `captured_stream_bytes` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | `test_streaming_reads_and_availability_agree` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently |
| `clear_captured_combined` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | `test_streaming_reads_and_availability_agree` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently |
| `clear_captured_stream` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | `test_streaming_reads_and_availability_agree` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently |
| `close` | implemented | `sync_process_close_is_idempotent` | `async_process_close_releases_the_actor_and_is_idempotent` | `test_sync_close_is_idempotent` | `test_async_close_is_idempotent_and_releases_the_handle` |
| `close_stdin` | implemented | `sync_process_close_is_idempotent` | `async_process_close_releases_the_actor_and_is_idempotent` | `test_start_pid_wait_report_a_consistent_lifecycle` | `test_stdin_write_then_close_delivers_eof` |
| `drain_combined` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | `test_streaming_reads_and_availability_agree` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently |
| `drain_stream` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | `test_streaming_reads_and_availability_agree` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently |
| `drain_stream_raw` | implemented | `raw_stream_drain_preserves_delimiters_non_utf8_and_eof` | n/a: the async answer is the output cursor; this synchronous byte-exact queue is intentionally consuming | n/a: no lossy Python binding is exposed for this byte-exact Rust API | n/a: no lossy Python binding is exposed for this byte-exact Rust API |
| `has_pending_combined` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | `test_streaming_reads_and_availability_agree` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently |
| `has_pending_stream` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | `test_streaming_reads_and_availability_agree` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently |
| `kill` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | `async_process_poll_reports_none_before_exit_and_a_status_after` | `test_kill_ends_a_running_child` | `test_kill_ends_a_running_child` |
| `new` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | `async_process_poll_reports_none_before_exit_and_a_status_after` | `test_start_pid_wait_report_a_consistent_lifecycle` | `test_start_pid_wait_report_a_consistent_lifecycle` |
| `pid` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | `async_process_poll_reports_none_before_exit_and_a_status_after` | `test_start_pid_wait_report_a_consistent_lifecycle` | `test_start_pid_wait_report_a_consistent_lifecycle` |
| `poll` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | `async_process_poll_reports_none_before_exit_and_a_status_after` | `test_sync_poll_reports_none_while_running_and_a_code_after_exit` | `test_async_poll_reports_none_while_running_and_a_code_after_exit` |
| `process_observation_capabilities` | implemented | `exact_trace_observes_rapid_execs_and_normalizes_minus_one` | n/a: capability discovery is an immediate host fact shared by both process facades | `test_capability_report_is_honest_for_host` | `test_async_cursor_is_opened_before_exact_launch` |
| `read_combined` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | `test_streaming_reads_and_availability_agree` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently |
| `read_stream` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | `test_streaming_reads_and_availability_agree` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently |
| `returncode` | implemented | `sync_process_returncode_is_none_until_the_child_exits` | `async_process_returncode_matches_the_exit_code_the_child_chose` | `test_sync_returncode_is_none_while_the_child_runs` | `test_async_returncode_matches_the_code_the_child_chose` |
| `start` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | `async_process_poll_reports_none_before_exit_and_a_status_after` | `test_start_pid_wait_report_a_consistent_lifecycle` | `test_start_pid_wait_report_a_consistent_lifecycle` |
| `terminate` | implemented | `sync_process_terminate_ends_the_child_like_kill` | `async_process_terminate_ends_the_child_like_kill` | `test_sync_terminate_ends_the_child_like_kill` | `test_async_terminate_ends_the_child_like_kill` |
| `terminate_group_soft` | implemented | `sync_terminate_group_soft_signals_a_group_the_child_owns` | `terminate_group_soft_signals_a_group_the_child_owns` | n/a: RunningProcess exposes no process-group terminate; the sync Python surface has never had one and #849 governs adding to it | `test_async_terminate_group_soft_signals_an_owned_group` |
| `wait` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | `async_process_poll_reports_none_before_exit_and_a_status_after` | `test_start_pid_wait_report_a_consistent_lifecycle` | `test_start_pid_wait_report_a_consistent_lifecycle` |
| `with_observer` | implemented | `a_spawned_tree_reports_its_direct_child_starting_and_exiting` | n/a: the observer subscription is a sync-capture notification channel; the async engine reports the same events through the output cursor | n/a: not exposed to Python on either surface; it is internal plumbing of the Rust engine | n/a: not exposed to Python on either surface; it is internal plumbing of the Rust engine |
| `with_observer_and_command` | implemented | `an_observed_configured_command_spawns_verbatim_and_reports_lifecycle` | n/a: the observer subscription is a sync-capture notification channel; the async engine reports the same events through the output cursor | n/a: not exposed to Python on either surface; a caller-configured std::process::Command has no Python spelling | n/a: not exposed to Python on either surface; a caller-configured std::process::Command has no Python spelling |
| `with_process_watches` | implemented | `exact_trace_observes_rapid_execs_and_normalizes_minus_one` | n/a: the watched async Python facade dispatches this same NativeProcess through the bounded blocking island | `test_add_watch_is_pre_start_only` | `test_async_cursor_is_opened_before_exact_launch` |
| `write_stdin` | implemented | `sync_process_close_is_idempotent` | `async_process_close_releases_the_actor_and_is_idempotent` | `test_start_pid_wait_report_a_consistent_lifecycle` | `test_stdin_write_then_close_delivers_eof` |
| `write_stdin_streaming` | implemented | `sync_process_close_is_idempotent` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | n/a: not exposed to Python on either surface; it is internal plumbing of the Rust engine | n/a: not exposed to Python on either surface; it is internal plumbing of the Rust engine |

### Rust `NativePtyProcess`

| Member | Status | Rust sync | Rust async | Python sync | Python async |
| --- | --- | --- | --- | --- | --- |
| `attach_idle_detector` | implemented | `sync_pty_idle_detector_attaches_and_detaches` | `async_pty_idle_detector_attaches_detaches_and_reports_an_outcome` | n/a: not exposed to Python on either surface; it is internal plumbing of the Rust engine | n/a: not exposed to Python on either surface; it is internal plumbing of the Rust engine |
| `close_impl` | implemented | `sync_pty_tree_termination_is_accepted_after_start` | `async_pty_tree_termination_dispatches_through_the_island` | `test_start_pid_read_wait_close_form_a_lifecycle` | `test_start_pid_read_wait_close_form_a_lifecycle` |
| `close_nonblocking` | implemented | `sync_pty_close_nonblocking_is_safe_before_start` | `async_pty_close_nonblocking_is_safe_before_start` | n/a: not exposed on PseudoTerminalProcess; the sync facade owns this bookkeeping internally rather than publishing it | `test_async_pty_close_nonblocking_is_safe_before_start` |
| `detach_idle_detector` | implemented | `sync_pty_idle_detector_attaches_and_detaches` | `async_pty_idle_detector_attaches_detaches_and_reports_an_outcome` | n/a: not exposed to Python on either surface; it is internal plumbing of the Rust engine | n/a: not exposed to Python on either surface; it is internal plumbing of the Rust engine |
| `echo_enabled` | implemented | `sync_pty_echo_state_round_trips` | `async_pty_echo_state_round_trips` | n/a: not exposed on PseudoTerminalProcess; the sync facade owns this bookkeeping internally rather than publishing it | `test_async_pty_echo_state_round_trips` |
| `kill_impl` | implemented | `sync_pty_tree_termination_is_accepted_after_start` | `async_pty_tree_termination_dispatches_through_the_island` | `test_terminate_and_kill_are_idempotent_after_exit` | `test_terminate_ends_the_child_and_a_following_kill_is_safe` |
| `kill_tree_impl` | implemented | `sync_pty_tree_termination_is_accepted_after_start` | `async_pty_tree_termination_dispatches_through_the_island` | n/a: PseudoTerminalProcess does not expose this PTY primitive; it is an internal detail of the sync facade's own read/wait loop | `test_async_pty_tree_termination_is_accepted_after_start` |
| `mark_reader_closed` | implemented | `sync_pty_store_returncode_and_mark_reader_closed_are_observable` | `async_pty_store_returncode_and_mark_reader_closed_are_observable` | n/a: not exposed on PseudoTerminalProcess; the sync facade owns this bookkeeping internally rather than publishing it | `test_async_pty_store_returncode_and_mark_reader_closed` |
| `new` | implemented | `sync_pty_tree_termination_is_accepted_after_start` | `async_pty_tree_termination_dispatches_through_the_island` | `test_start_pid_read_wait_close_form_a_lifecycle` | `test_start_pid_read_wait_close_form_a_lifecycle` |
| `pid` | implemented | `sync_pty_tree_termination_is_accepted_after_start` | `async_pty_tree_termination_dispatches_through_the_island` | `test_start_pid_read_wait_close_form_a_lifecycle` | `test_start_pid_read_wait_close_form_a_lifecycle` |
| `pty_control_churn_bytes_total` | implemented | `sync_pty_metrics_track_recorded_input` | `async_pty_metrics_track_recorded_input_like_the_sync_counters` | n/a: not exposed on PseudoTerminalProcess; the sync facade owns this bookkeeping internally rather than publishing it | `test_async_pty_metrics_track_recorded_input` |
| `pty_input_bytes_total` | implemented | `sync_pty_metrics_track_recorded_input` | `async_pty_metrics_track_recorded_input_like_the_sync_counters` | `test_sync_pty_output_bytes_start_at_zero` | `test_async_pty_metrics_track_recorded_input` |
| `pty_newline_events_total` | implemented | `sync_pty_metrics_track_recorded_input` | `async_pty_metrics_track_recorded_input_like_the_sync_counters` | n/a: not exposed on PseudoTerminalProcess; the sync facade owns this bookkeeping internally rather than publishing it | `test_async_pty_metrics_track_recorded_input` |
| `pty_output_bytes_total` | implemented | `sync_pty_metrics_track_recorded_input` | `async_pty_metrics_track_recorded_input_like_the_sync_counters` | `test_sync_pty_output_bytes_start_at_zero` | `test_async_pty_metrics_track_recorded_input` |
| `pty_submit_events_total` | implemented | `sync_pty_metrics_track_recorded_input` | `async_pty_metrics_track_recorded_input_like_the_sync_counters` | n/a: not exposed on PseudoTerminalProcess; the sync facade owns this bookkeeping internally rather than publishing it | `test_async_pty_metrics_track_recorded_input` |
| `read_chunk_impl` | implemented | `sync_pty_tree_termination_is_accepted_after_start` | `async_pty_tree_termination_dispatches_through_the_island` | `test_start_pid_read_wait_close_form_a_lifecycle` | `test_start_pid_read_wait_close_form_a_lifecycle` |
| `record_input_metrics` | implemented | `sync_pty_metrics_track_recorded_input` | `async_pty_metrics_track_recorded_input_like_the_sync_counters` | `test_sync_pty_output_bytes_start_at_zero` | `test_async_pty_metrics_track_recorded_input` |
| `request_terminal_input_relay_stop` | implemented | `sync_pty_relay_is_inactive_until_started_and_stops_cleanly` | `async_pty_relay_is_inactive_until_started_and_stops_cleanly` | `test_sync_pty_terminal_input_relay_is_inactive_until_started` | `test_async_pty_relay_is_inactive_until_started` |
| `resize_impl` | implemented | `sync_pty_tree_termination_is_accepted_after_start` | `async_pty_tree_termination_dispatches_through_the_island` | `test_start_pid_read_wait_close_form_a_lifecycle` | `test_start_pid_read_wait_close_form_a_lifecycle` |
| `respond_to_queries_impl` | implemented | `sync_pty_respond_to_queries_without_a_query_is_a_noop` | `async_pty_respond_to_queries_without_a_query_is_a_noop` | n/a: PseudoTerminalProcess does not expose this PTY primitive; it is an internal detail of the sync facade's own read/wait loop | `test_async_pty_respond_to_queries_without_a_query_is_a_noop` |
| `send_interrupt_impl` | implemented | `sync_pty_send_interrupt_before_start_errors` | `async_pty_send_interrupt_before_start_errors` | `test_sync_pty_send_interrupt_before_start_is_rejected` | `test_async_pty_send_interrupt_before_start_raises` |
| `set_echo` | implemented | `sync_pty_echo_state_round_trips` | `async_pty_echo_state_round_trips` | n/a: not exposed on PseudoTerminalProcess; the sync facade owns this bookkeeping internally rather than publishing it | `test_async_pty_echo_state_round_trips` |
| `start_impl` | implemented | `sync_pty_tree_termination_is_accepted_after_start` | `async_pty_tree_termination_dispatches_through_the_island` | `test_start_pid_read_wait_close_form_a_lifecycle` | `test_start_pid_read_wait_close_form_a_lifecycle` |
| `start_terminal_input_relay_impl` | implemented | `sync_pty_start_terminal_input_relay_requires_a_running_pty` | `async_pty_start_terminal_input_relay_requires_a_running_pty` | n/a: PseudoTerminalProcess does not expose this PTY primitive; it is an internal detail of the sync facade's own read/wait loop | `test_async_pty_start_relay_requires_a_running_pty` |
| `stop_terminal_input_relay_impl` | implemented | `sync_pty_relay_is_inactive_until_started_and_stops_cleanly` | `async_pty_relay_is_inactive_until_started_and_stops_cleanly` | `test_sync_pty_terminal_input_relay_is_inactive_until_started` | `test_async_pty_relay_is_inactive_until_started` |
| `store_returncode` | implemented | `sync_pty_store_returncode_and_mark_reader_closed_are_observable` | `async_pty_store_returncode_and_mark_reader_closed_are_observable` | n/a: not exposed on PseudoTerminalProcess; the sync facade owns this bookkeeping internally rather than publishing it | `test_async_pty_store_returncode_and_mark_reader_closed` |
| `terminal_input_relay_active` | implemented | `sync_pty_relay_is_inactive_until_started_and_stops_cleanly` | `async_pty_relay_is_inactive_until_started_and_stops_cleanly` | `test_sync_pty_terminal_input_relay_is_inactive_until_started` | `test_async_pty_relay_is_inactive_until_started` |
| `terminate_impl` | implemented | `sync_pty_tree_termination_is_accepted_after_start` | `async_pty_tree_termination_dispatches_through_the_island` | `test_terminate_and_kill_are_idempotent_after_exit` | `test_terminate_ends_the_child_and_a_following_kill_is_safe` |
| `terminate_tree_impl` | implemented | `sync_pty_tree_termination_is_accepted_after_start` | `async_pty_tree_termination_dispatches_through_the_island` | n/a: PseudoTerminalProcess does not expose this PTY primitive; it is an internal detail of the sync facade's own read/wait loop | `test_async_pty_tree_termination_is_accepted_after_start` |
| `wait_and_drain_impl` | implemented | `sync_pty_wait_and_drain_agrees_with_wait_on_an_exited_child` | `async_pty_wait_and_drain_agrees_with_wait_on_an_exited_child` | n/a: PseudoTerminalProcess does not expose this PTY primitive; it is an internal detail of the sync facade's own read/wait loop | `test_async_pty_wait_and_drain_agrees_with_wait` |
| `wait_for_reader_closed_impl` | implemented | `sync_pty_wait_for_reader_closed_reports_a_bounded_timeout` | `async_pty_wait_for_reader_closed_reports_a_bounded_timeout` | n/a: PseudoTerminalProcess does not expose this PTY primitive; it is an internal detail of the sync facade's own read/wait loop | `test_async_pty_wait_for_reader_closed_is_bounded` |
| `wait_impl` | implemented | `sync_pty_tree_termination_is_accepted_after_start` | `async_pty_tree_termination_dispatches_through_the_island` | `test_start_pid_read_wait_close_form_a_lifecycle` | `test_start_pid_read_wait_close_form_a_lifecycle` |
| `write_impl` | implemented | `sync_pty_tree_termination_is_accepted_after_start` | `async_pty_tree_termination_dispatches_through_the_island` | `test_start_pid_read_wait_close_form_a_lifecycle` | `test_start_pid_read_wait_close_form_a_lifecycle` |

### Python `RunningProcess`

| Member | Status | Rust sync | Rust async | Python sync | Python async |
| --- | --- | --- | --- | --- | --- |
| `add_process_watch` | implemented | `exact_trace_observes_rapid_execs_and_normalizes_minus_one` | n/a: watch mutation is deliberately pre-start and synchronous on both facades | `test_add_watch_is_pre_start_only` | n/a: AsyncRunningProcess takes its immutable watch sequence at construction |
| `captured_output_bytes` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | `test_captured_output_bytes_and_discard_release_history` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently |
| `checkpoint` | implemented | n/a: a Python facade concern with no Rust counterpart; the Rust columns of the underlying pipe/PTY rows carry the engine contract | n/a: a Python facade concern with no Rust counterpart; the Rust columns of the underlying pipe/PTY rows carry the engine contract | `test_sync_expect_matches_a_literal_in_process_output` | `test_expect_matches_a_literal_in_process_output` |
| `close` | implemented | `sync_process_close_is_idempotent` | `async_process_close_releases_the_actor_and_is_idempotent` | `test_sync_close_is_idempotent` | `test_async_close_is_idempotent_and_releases_the_handle` |
| `combined_output` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | `test_captured_output_carries_both_streams` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently |
| `combined_stream` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | `test_captured_output_carries_both_streams` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently |
| `discard_captured_output` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | `test_captured_output_bytes_and_discard_release_history` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently |
| `drain_combined` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | `test_streaming_reads_and_availability_agree` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently |
| `drain_stderr` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | `test_streaming_reads_and_availability_agree` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently |
| `drain_stdout` | implemented | n/a: the Rust sync counterpart is NativeProcess::read_stream / drain_stream, tracked on their own rows | `async_cursor_returns_terminal_eof_after_close` | `test_sync_drain_stdout_consumes_what_it_returns` | `test_two_cursors_read_the_same_records_independently` |
| `duration` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: a synchronous accessor over already-observed state; there is nothing to await, and an async form would only add a round trip | `test_metadata_is_populated_after_a_run` | n/a: a synchronous accessor over already-observed state; there is nothing to await, and an async form would only add a round trip |
| `end_time` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: a synchronous accessor over already-observed state; there is nothing to await, and an async form would only add a round trip | `test_metadata_is_populated_after_a_run` | n/a: a synchronous accessor over already-observed state; there is nothing to await, and an async form would only add a round trip |
| `exec_script` | implemented | n/a: a constructor/classmethod, not an operation; the async counterpart is the corresponding async class's own constructor | n/a: a constructor/classmethod, not an operation; the async counterpart is the corresponding async class's own constructor | `test_start_pid_wait_report_a_consistent_lifecycle` | n/a: a constructor/classmethod, not an operation; the async counterpart is the corresponding async class's own constructor |
| `exit_status` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: a synchronous accessor over already-observed state; there is nothing to await, and an async form would only add a round trip | `test_metadata_is_populated_after_a_run` | n/a: a synchronous accessor over already-observed state; there is nothing to await, and an async form would only add a round trip |
| `expect` | implemented | n/a: expect has no Rust implementation on either surface; it is Python code over native reads on both, so the Python columns are the whole contract | n/a: expect has no Rust implementation on either surface; it is Python code over native reads on both, so the Python columns are the whole contract | `test_sync_expect_matches_a_literal_in_process_output` | `test_expect_matches_a_literal_in_process_output` |
| `finished` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | `async_process_poll_reports_none_before_exit_and_a_status_after` | `test_start_pid_wait_report_a_consistent_lifecycle` | `test_start_pid_wait_report_a_consistent_lifecycle` |
| `get_command_str` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: a synchronous accessor over already-observed state; there is nothing to await, and an async form would only add a round trip | `test_metadata_is_populated_after_a_run` | n/a: a synchronous accessor over already-observed state; there is nothing to await, and an async form would only add a round trip |
| `get_next_line` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | `test_streaming_reads_and_availability_agree` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently |
| `get_next_line_non_blocking` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | `test_streaming_reads_and_availability_agree` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently |
| `get_next_stderr_line` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | `test_streaming_reads_and_availability_agree` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently |
| `get_next_stdout_line` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | `test_streaming_reads_and_availability_agree` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently |
| `has_pending_output` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | `test_streaming_reads_and_availability_agree` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently |
| `has_pending_stderr` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | `test_streaming_reads_and_availability_agree` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently |
| `has_pending_stdout` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | `test_streaming_reads_and_availability_agree` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently |
| `idle_timeout_enabled` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: the sync idle switch is a property on a detector the async facade does not require; the async form takes the quiet window as an argument, covered by test_wait_for_idle_reports_true_once_output_stops | `test_pseudo_terminal_idle_timeout_signal_can_be_reenabled_during_wait` | n/a: the sync idle switch is a property on a detector the async facade does not require; the async form takes the quiet window as an argument, covered by test_wait_for_idle_reports_true_once_output_stops |
| `interactive` | implemented | n/a: a constructor/classmethod, not an operation; the async counterpart is the corresponding async class's own constructor | n/a: a constructor/classmethod, not an operation; the async counterpart is the corresponding async class's own constructor | `test_start_pid_wait_report_a_consistent_lifecycle` | n/a: a constructor/classmethod, not an operation; the async counterpart is the corresponding async class's own constructor |
| `interactive_launch_spec` | implemented | n/a: a constructor/classmethod, not an operation; the async counterpart is the corresponding async class's own constructor | n/a: a constructor/classmethod, not an operation; the async counterpart is the corresponding async class's own constructor | `test_start_pid_wait_report_a_consistent_lifecycle` | n/a: a constructor/classmethod, not an operation; the async counterpart is the corresponding async class's own constructor |
| `is_running` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | `async_process_poll_reports_none_before_exit_and_a_status_after` | `test_start_pid_wait_report_a_consistent_lifecycle` | `test_start_pid_wait_report_a_consistent_lifecycle` |
| `is_runninng` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | `async_process_poll_reports_none_before_exit_and_a_status_after` | `test_the_compat_alias_reports_what_the_real_spelling_does` | `test_start_pid_wait_report_a_consistent_lifecycle` |
| `is_started` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | `async_process_poll_reports_none_before_exit_and_a_status_after` | `test_start_pid_wait_report_a_consistent_lifecycle` | `test_start_pid_wait_report_a_consistent_lifecycle` |
| `kill` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | `async_process_poll_reports_none_before_exit_and_a_status_after` | `test_kill_ends_a_running_child` | `test_kill_ends_a_running_child` |
| `line_iter` | implemented | n/a: the Rust sync counterpart is NativeProcess::read_stream / drain_stream, tracked on their own rows | `async_cursor_returns_terminal_eof_after_close` | `test_sync_stream_iter_yields_the_emitted_lines` | `test_cursor_streams_records_until_eof` |
| `pid` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | `async_process_poll_reports_none_before_exit_and_a_status_after` | `test_start_pid_wait_report_a_consistent_lifecycle` | `test_start_pid_wait_report_a_consistent_lifecycle` |
| `poll` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | `async_process_poll_reports_none_before_exit_and_a_status_after` | `test_sync_poll_reports_none_while_running_and_a_code_after_exit` | `test_async_poll_reports_none_while_running_and_a_code_after_exit` |
| `proc` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: a synchronous accessor over already-observed state; there is nothing to await, and an async form would only add a round trip | `test_metadata_is_populated_after_a_run` | n/a: a synchronous accessor over already-observed state; there is nothing to await, and an async form would only add a round trip |
| `process_observation` | implemented | `exact_trace_observes_rapid_execs_and_normalizes_minus_one` | n/a: observation metadata is an immediate immutable value shared by both facades | `test_non_invasive_selection_never_claims_exact` | `test_async_cursor_is_opened_before_exact_launch` |
| `process_observation_capabilities` | implemented | `exact_trace_observes_rapid_execs_and_normalizes_minus_one` | n/a: capability discovery is an immediate host fact shared by both process facades | `test_capability_report_is_honest_for_host` | `test_async_cursor_is_opened_before_exact_launch` |
| `process_watch_cursor` | implemented | `bounded_log_reports_an_explicit_cursor_gap` | n/a: the async Python cursor awaits the same Rust watch log through the bounded blocking island | `test_add_watch_is_pre_start_only` | `test_async_cursor_is_opened_before_exact_launch` |
| `pseudo_terminal` | implemented | n/a: a constructor/classmethod, not an operation; the async counterpart is the corresponding async class's own constructor | n/a: a constructor/classmethod, not an operation; the async counterpart is the corresponding async class's own constructor | `test_start_pid_wait_report_a_consistent_lifecycle` | n/a: a constructor/classmethod, not an operation; the async counterpart is the corresponding async class's own constructor |
| `returncode` | implemented | `sync_process_returncode_is_none_until_the_child_exits` | `async_process_returncode_matches_the_exit_code_the_child_chose` | `test_sync_returncode_is_none_while_the_child_runs` | `test_async_returncode_matches_the_code_the_child_chose` |
| `run` | implemented | n/a: no Rust free-function counterpart; this is a Python-level convenience over capabilities the Rust columns of the tree rows already cover | n/a: no Rust free-function counterpart; this is a Python-level convenience over capabilities the Rust columns of the tree rows already cover | `test_subprocess_run_capture_output` | `test_subprocess_run_matches_the_sync_helper_result` |
| `run_streaming` | implemented | n/a: a constructor/classmethod, not an operation; the async counterpart is the corresponding async class's own constructor | n/a: a constructor/classmethod, not an operation; the async counterpart is the corresponding async class's own constructor | `test_start_pid_wait_report_a_consistent_lifecycle` | n/a: a constructor/classmethod, not an operation; the async counterpart is the corresponding async class's own constructor |
| `send_interrupt` | implemented | `sync_pty_send_interrupt_before_start_errors` | `async_pty_send_interrupt_before_start_errors` | `test_sync_pty_send_interrupt_before_start_is_rejected` | `test_pipe_session_rejects_pty_only_operations` |
| `start` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | `async_process_poll_reports_none_before_exit_and_a_status_after` | `test_start_pid_wait_report_a_consistent_lifecycle` | `test_start_pid_wait_report_a_consistent_lifecycle` |
| `start_time` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: a synchronous accessor over already-observed state; there is nothing to await, and an async form would only add a round trip | `test_metadata_is_populated_after_a_run` | n/a: a synchronous accessor over already-observed state; there is nothing to await, and an async form would only add a round trip |
| `stderr` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | `test_captured_output_carries_both_streams` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently |
| `stderr_stream` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | `test_captured_output_carries_both_streams` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently |
| `stdout` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | `test_captured_output_carries_both_streams` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently |
| `stdout_stream` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | `test_captured_output_carries_both_streams` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently |
| `stream_iter` | implemented | n/a: the Rust sync counterpart is NativeProcess::read_stream / drain_stream, tracked on their own rows | `async_cursor_returns_terminal_eof_after_close` | `test_sync_stream_iter_yields_the_emitted_lines` | `test_cursor_is_an_async_iterator` |
| `submit` | implemented | `sync_process_close_is_idempotent` | `async_process_close_releases_the_actor_and_is_idempotent` | `test_write_then_submit_delivers_input_to_the_child` | `test_stdin_write_then_close_delivers_eof` |
| `terminate` | implemented | `sync_process_terminate_ends_the_child_like_kill` | `async_process_terminate_ends_the_child_like_kill` | `test_sync_terminate_ends_the_child_like_kill` | `test_async_terminate_ends_the_child_like_kill` |
| `wait` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | `async_process_poll_reports_none_before_exit_and_a_status_after` | `test_start_pid_wait_report_a_consistent_lifecycle` | `test_start_pid_wait_report_a_consistent_lifecycle` |
| `wait_for` | implemented | n/a: a Python facade concern with no Rust counterpart; the Rust columns of the underlying pipe/PTY rows carry the engine contract | n/a: a Python facade concern with no Rust counterpart; the Rust columns of the underlying pipe/PTY rows carry the engine contract | `test_sync_expect_matches_a_literal_in_process_output` | `test_expect_matches_a_literal_in_process_output` |
| `wait_for_expect` | implemented | n/a: a Python facade concern with no Rust counterpart; the Rust columns of the underlying pipe/PTY rows carry the engine contract | n/a: a Python facade concern with no Rust counterpart; the Rust columns of the underlying pipe/PTY rows carry the engine contract | `test_sync_expect_matches_a_literal_in_process_output` | `test_expect_matches_a_literal_in_process_output` |
| `wait_for_idle` | implemented | n/a: expect has no Rust implementation on either surface; it is Python code over native reads on both, so the Python columns are the whole contract | n/a: expect has no Rust implementation on either surface; it is Python code over native reads on both, so the Python columns are the whole contract | `test_sync_expect_matches_a_literal_in_process_output` | `test_wait_for_idle_reports_true_once_output_stops` |
| `watch_matches` | implemented | `exact_trace_observes_rapid_execs_and_normalizes_minus_one` | n/a: retained matches are an immediate immutable snapshot shared by both facades | `test_non_invasive_selection_never_claims_exact` | `test_async_cursor_is_opened_before_exact_launch` |
| `write` | implemented | `sync_process_close_is_idempotent` | `async_process_close_releases_the_actor_and_is_idempotent` | `test_write_then_submit_delivers_input_to_the_child` | `test_stdin_write_then_close_delivers_eof` |

### Python `PseudoTerminalProcess`

| Member | Status | Rust sync | Rust async | Python sync | Python async |
| --- | --- | --- | --- | --- | --- |
| `available` | implemented | `sync_pty_tree_termination_is_accepted_after_start` | n/a: a synchronous accessor over already-observed state; there is nothing to await, and an async form would only add a round trip | `test_start_pid_read_wait_close_form_a_lifecycle` | n/a: a synchronous accessor over already-observed state; there is nothing to await, and an async form would only add a round trip |
| `checkpoint` | implemented | n/a: a Python facade concern with no Rust counterpart; the Rust columns of the underlying pipe/PTY rows carry the engine contract | n/a: a Python facade concern with no Rust counterpart; the Rust columns of the underlying pipe/PTY rows carry the engine contract | `test_sync_pty_expect_matches_child_output` | `test_pty_expect_matches_child_output` |
| `close` | implemented | `sync_pty_tree_termination_is_accepted_after_start` | `async_pty_tree_termination_dispatches_through_the_island` | `test_start_pid_read_wait_close_form_a_lifecycle` | `test_start_pid_read_wait_close_form_a_lifecycle` |
| `discard_output` | implemented | `sync_pty_tree_termination_is_accepted_after_start` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | `test_reads_and_discard_release_history` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently |
| `drain` | implemented | `sync_pty_tree_termination_is_accepted_after_start` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | `test_reads_and_discard_release_history` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently |
| `drain_echo` | implemented | `sync_pty_tree_termination_is_accepted_after_start` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | `test_reads_and_discard_release_history` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently |
| `exit_status` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: a synchronous accessor over already-observed state; there is nothing to await, and an async form would only add a round trip | `test_start_pid_read_wait_close_form_a_lifecycle` | n/a: a synchronous accessor over already-observed state; there is nothing to await, and an async form would only add a round trip |
| `expect` | implemented | n/a: expect has no Rust implementation on either surface; it is Python code over native reads on both, so the Python columns are the whole contract | n/a: expect has no Rust implementation on either surface; it is Python code over native reads on both, so the Python columns are the whole contract | `test_sync_pty_expect_matches_child_output` | `test_pty_expect_matches_child_output` |
| `idle_timeout_enabled` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | n/a: the sync idle switch is a property on a detector the async facade does not require; the async form takes the quiet window as an argument, covered by test_wait_for_idle_reports_true_once_output_stops | `test_pseudo_terminal_idle_timeout_signal_can_be_reenabled_during_wait` | n/a: the sync idle switch is a property on a detector the async facade does not require; the async form takes the quiet window as an argument, covered by test_wait_for_idle_reports_true_once_output_stops |
| `interrupt_and_wait` | implemented | `sync_pty_send_interrupt_before_start_errors` | `async_pty_send_interrupt_before_start_errors` | `test_pseudo_terminal_interrupt_and_wait_reports_second_interrupt_success` | `test_async_pty_send_interrupt_before_start_raises` |
| `is_running` | implemented | `sync_pty_tree_termination_is_accepted_after_start` | `async_pty_tree_termination_dispatches_through_the_island` | `test_start_pid_read_wait_close_form_a_lifecycle` | `test_start_pid_read_wait_close_form_a_lifecycle` |
| `kill` | implemented | `sync_pty_tree_termination_is_accepted_after_start` | `async_pty_tree_termination_dispatches_through_the_island` | `test_terminate_and_kill_are_idempotent_after_exit` | `test_terminate_ends_the_child_and_a_following_kill_is_safe` |
| `output` | implemented | `sync_pty_tree_termination_is_accepted_after_start` | `async_pty_tree_termination_dispatches_through_the_island` | `test_start_pid_read_wait_close_form_a_lifecycle` | `test_start_pid_read_wait_close_form_a_lifecycle` |
| `output_bytes` | implemented | `sync_pty_metrics_track_recorded_input` | `async_pty_metrics_track_recorded_input_like_the_sync_counters` | `test_sync_pty_output_bytes_start_at_zero` | `test_async_pty_metrics_track_recorded_input` |
| `output_text` | implemented | `sync_pty_tree_termination_is_accepted_after_start` | `async_pty_tree_termination_dispatches_through_the_island` | `test_start_pid_read_wait_close_form_a_lifecycle` | `test_start_pid_read_wait_close_form_a_lifecycle` |
| `pid` | implemented | `sync_pty_tree_termination_is_accepted_after_start` | `async_pty_tree_termination_dispatches_through_the_island` | `test_start_pid_read_wait_close_form_a_lifecycle` | `test_start_pid_read_wait_close_form_a_lifecycle` |
| `poll` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | `async_process_poll_reports_none_before_exit_and_a_status_after` | `test_terminate_and_kill_are_idempotent_after_exit` | `test_poll_reports_none_while_running_and_a_code_after_exit` |
| `read` | implemented | `sync_pty_tree_termination_is_accepted_after_start` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | `test_reads_and_discard_release_history` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently |
| `read_non_blocking` | implemented | `sync_pty_tree_termination_is_accepted_after_start` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | `test_reads_and_discard_release_history` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently |
| `read_text` | implemented | `sync_pty_tree_termination_is_accepted_after_start` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently | `test_reads_and_discard_release_history` | n/a: the async answer to the consuming drain/read family is the output cursor, which gives each reader its own position instead of a shared buffer one caller empties; covered by test_two_cursors_read_the_same_records_independently |
| `resize` | implemented | `sync_pty_tree_termination_is_accepted_after_start` | `async_pty_tree_termination_dispatches_through_the_island` | `test_start_pid_read_wait_close_form_a_lifecycle` | `test_start_pid_read_wait_close_form_a_lifecycle` |
| `send_interrupt` | implemented | `sync_pty_send_interrupt_before_start_errors` | `async_pty_send_interrupt_before_start_errors` | `test_sync_pty_send_interrupt_before_start_is_rejected` | `test_async_pty_send_interrupt_before_start_raises` |
| `start` | implemented | `sync_pty_tree_termination_is_accepted_after_start` | `async_pty_tree_termination_dispatches_through_the_island` | `test_start_pid_read_wait_close_form_a_lifecycle` | `test_start_pid_read_wait_close_form_a_lifecycle` |
| `start_terminal_input_relay` | implemented | `sync_pty_start_terminal_input_relay_requires_a_running_pty` | `async_pty_start_terminal_input_relay_requires_a_running_pty` | `test_sync_pty_terminal_input_relay_is_inactive_until_started` | `test_async_pty_start_relay_requires_a_running_pty` |
| `stop_terminal_input_relay` | implemented | `sync_pty_relay_is_inactive_until_started_and_stops_cleanly` | `async_pty_relay_is_inactive_until_started_and_stops_cleanly` | `test_sync_pty_terminal_input_relay_is_inactive_until_started` | `test_async_pty_relay_is_inactive_until_started` |
| `submit` | implemented | `sync_pty_tree_termination_is_accepted_after_start` | `async_pty_tree_termination_dispatches_through_the_island` | `test_resize_and_write_are_accepted_on_a_running_pty` | `test_write_reaches_the_child` |
| `terminal_input_relay_active` | implemented | `sync_pty_relay_is_inactive_until_started_and_stops_cleanly` | `async_pty_relay_is_inactive_until_started_and_stops_cleanly` | `test_sync_pty_terminal_input_relay_is_inactive_until_started` | `test_async_pty_relay_is_inactive_until_started` |
| `terminate` | implemented | `sync_pty_tree_termination_is_accepted_after_start` | `async_pty_tree_termination_dispatches_through_the_island` | `test_terminate_and_kill_are_idempotent_after_exit` | `test_terminate_ends_the_child_and_a_following_kill_is_safe` |
| `wait` | implemented | `sync_pty_tree_termination_is_accepted_after_start` | `async_pty_tree_termination_dispatches_through_the_island` | `test_start_pid_read_wait_close_form_a_lifecycle` | `test_start_pid_read_wait_close_form_a_lifecycle` |
| `wait_for` | implemented | n/a: a Python facade concern with no Rust counterpart; the Rust columns of the underlying pipe/PTY rows carry the engine contract | n/a: a Python facade concern with no Rust counterpart; the Rust columns of the underlying pipe/PTY rows carry the engine contract | `test_sync_pty_expect_matches_child_output` | `test_pty_expect_matches_child_output` |
| `wait_for_expect` | implemented | n/a: a Python facade concern with no Rust counterpart; the Rust columns of the underlying pipe/PTY rows carry the engine contract | n/a: a Python facade concern with no Rust counterpart; the Rust columns of the underlying pipe/PTY rows carry the engine contract | `test_sync_pty_expect_matches_child_output` | `test_pty_expect_matches_child_output` |
| `wait_for_idle` | implemented | `sync_pty_idle_detector_attaches_and_detaches` | `async_pty_idle_detector_attaches_detaches_and_reports_an_outcome` | `test_pseudo_terminal_wait_for_idle_can_arm_on_explicit_input_submit` | `test_pty_wait_for_output_idle_reports_true_once_quiet` |
| `write` | implemented | `sync_pty_tree_termination_is_accepted_after_start` | `async_pty_tree_termination_dispatches_through_the_island` | `test_resize_and_write_are_accepted_on_a_running_pty` | `test_write_reaches_the_child` |

### Python `InteractiveProcess`

| Member | Status | Rust sync | Rust async | Python sync | Python async |
| --- | --- | --- | --- | --- | --- |
| `close` | implemented | n/a: InteractiveProcess is a Python-only dispatch facade; Rust exposes the pipe and PTY engines separately, tracked on their own surfaces | n/a: InteractiveProcess is a Python-only dispatch facade; Rust exposes the pipe and PTY engines separately, tracked on their own surfaces | `test_sync_close_is_idempotent` | `test_async_close_is_idempotent_and_releases_the_handle` |
| `exit_status` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | `async_process_poll_reports_none_before_exit_and_a_status_after` | `test_sync_poll_reports_none_before_start` | `test_exit_status_agrees_with_poll` |
| `kill` | implemented | n/a: InteractiveProcess is a Python-only dispatch facade; Rust exposes the pipe and PTY engines separately, tracked on their own surfaces | n/a: InteractiveProcess is a Python-only dispatch facade; Rust exposes the pipe and PTY engines separately, tracked on their own surfaces | `test_sync_poll_reports_none_before_start` | `test_poll_reports_none_while_running_and_a_code_after_exit` |
| `pid` | implemented | n/a: InteractiveProcess is a Python-only dispatch facade; Rust exposes the pipe and PTY engines separately, tracked on their own surfaces | n/a: InteractiveProcess is a Python-only dispatch facade; Rust exposes the pipe and PTY engines separately, tracked on their own surfaces | `test_sync_poll_reports_none_before_start` | `test_pipe_session_kill_tree_reports_a_count` |
| `poll` | implemented | `sync_process_poll_reports_none_before_exit_and_a_code_after` | `async_process_poll_reports_none_before_exit_and_a_status_after` | `test_sync_poll_reports_none_before_start` | `test_poll_reports_none_while_running_and_a_code_after_exit` |
| `send_interrupt` | implemented | n/a: InteractiveProcess is a Python-only dispatch facade; Rust exposes the pipe and PTY engines separately, tracked on their own surfaces | n/a: InteractiveProcess is a Python-only dispatch facade; Rust exposes the pipe and PTY engines separately, tracked on their own surfaces | `test_sync_pty_send_interrupt_before_start_is_rejected` | `test_pipe_session_rejects_pty_only_operations` |
| `start` | implemented | n/a: InteractiveProcess is a Python-only dispatch facade; Rust exposes the pipe and PTY engines separately, tracked on their own surfaces | n/a: InteractiveProcess is a Python-only dispatch facade; Rust exposes the pipe and PTY engines separately, tracked on their own surfaces | `test_sync_poll_reports_none_before_start` | `test_poll_reports_none_while_running_and_a_code_after_exit` |
| `terminate` | implemented | n/a: InteractiveProcess is a Python-only dispatch facade; Rust exposes the pipe and PTY engines separately, tracked on their own surfaces | n/a: InteractiveProcess is a Python-only dispatch facade; Rust exposes the pipe and PTY engines separately, tracked on their own surfaces | `test_sync_terminate_ends_the_child_like_kill` | `test_async_terminate_ends_the_child_like_kill` |
| `wait` | implemented | n/a: InteractiveProcess is a Python-only dispatch facade; Rust exposes the pipe and PTY engines separately, tracked on their own surfaces | n/a: InteractiveProcess is a Python-only dispatch facade; Rust exposes the pipe and PTY engines separately, tracked on their own surfaces | `test_sync_poll_reports_none_before_start` | `test_exit_status_agrees_with_poll` |

### Python module-level helpers

| Member | Status | Rust sync | Rust async | Python sync | Python async |
| --- | --- | --- | --- | --- | --- |
| `find_processes_by_originator` | implemented | n/a: no Rust free-function counterpart; this is a Python-level convenience over capabilities the Rust columns of the tree rows already cover | n/a: no Rust free-function counterpart; this is a Python-level convenience over capabilities the Rust columns of the tree rows already cover | `test_process_utils_describe_current_process` | `test_find_processes_by_originator_matches_the_sync_helper` |
| `get_process_tree_info` | implemented | n/a: no Rust free-function counterpart; this is a Python-level convenience over capabilities the Rust columns of the tree rows already cover | n/a: no Rust free-function counterpart; this is a Python-level convenience over capabilities the Rust columns of the tree rows already cover | `test_process_utils_describe_current_process` | `test_get_process_tree_info_matches_the_sync_helper_shape` |
| `kill_process_tree` | implemented | `sync_kill_tree_terminates_the_started_child` | `missing_pid_is_a_successful_noop_on_the_async_form` | `test_kill_process_tree_kills_parent_and_child` | `test_module_level_kill_process_tree_kills_a_real_child` |
| `launch_detached` | implemented | n/a: no Rust free-function counterpart; this is a Python-level convenience over capabilities the Rust columns of the tree rows already cover | n/a: no Rust free-function counterpart; this is a Python-level convenience over capabilities the Rust columns of the tree rows already cover | `test_launch_detached_rejects_empty_command` | `test_launch_detached_matches_the_sync_helper_validation` |
| `subprocess_run` | implemented | n/a: no Rust free-function counterpart; this is a Python-level convenience over capabilities the Rust columns of the tree rows already cover | n/a: no Rust free-function counterpart; this is a Python-level convenience over capabilities the Rust columns of the tree rows already cover | `test_subprocess_run_capture_output` | `test_subprocess_run_matches_the_sync_helper_result` |
| `terminate_process_tree` | implemented | `sync_kill_tree_terminates_the_started_child` | `missing_pid_is_a_successful_noop_on_the_async_form` | `test_terminate_process_tree_matches_the_sync_helper_on_a_dead_pid` | `test_terminate_process_tree_ends_a_real_child` |

<!-- END GENERATED PARITY TABLE -->
