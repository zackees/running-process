[package]
name = "running-process-daemon-registration-pass"
version = "0.0.0"
edition = "2021"
publish = false

[workspace]

[dependencies]
running-process = { path = "../../..", default-features = false, features = ["daemon-registration"] }
