# Neutral platform capability indexes

These modules establish ownership names without choosing an operating system.
They may re-export a selected implementation once a capability migrates, but
must not contain host cfg predicates or native OS API references themselves.
